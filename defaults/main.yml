#SPDX-License-Identifier: MIT
---
# defaults file for ansible-role-ssh-hardening

# Group that AllowGroups gates SSH access on.
ssh_group: ssh-users

# fail2ban
fail2ban_bantime: 3600 # seconds — 1 hour
fail2ban_maxretry: 3
